# LF everywhere: this project targets Linux only, and a CRLF checkout or
# commit breaks every #!/usr/bin/env bash script and bpftrace probe with
# "bad interpreter". No binary files are tracked, so one rule covers the tree;
# every tracked file is already LF, so this forces no renormalization.
* text=auto eol=lf
